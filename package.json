{
  "name": "@krofdrakula/prop-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test -- --run",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@changesets/cli": "2.26.0"
  }
}
